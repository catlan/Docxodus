{
  "name": "docxodus-ts",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript port of the Docxodus DocxDiff IR engine (1:1 port of Docxodus/Ir + Ir/Diff; see ../docs/architecture/ir_diff_engine.md). Verified differentially against the C# WASM engine via deterministic edit-script JSON.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.1",
    "fflate": "^0.8.2",
    "saxes": "^6.0.0"
  },
  "devDependencies": {
    "docxodus": "workspace:*",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
